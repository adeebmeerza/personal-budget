describe("Envelope Transactions", () => {
  describe("List all transactions made", () => {
    it("returns all transactions involving a specific envelope", async () => {});

    it("returns all transactions made filter by transaction date", async () => {});

    it("returns all transactions involving a specific envelope as the source envelope", async () => {});

    it("returns all transactions involving a specific envelope as the destination envelope", async () => {});

    it("returns error 404 if no filter conditions met", async () => {});
  });
});
